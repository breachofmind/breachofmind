version: 1
applications:
  -
    frontend:
      phases:
        preBuild:
          commands: ['npm install']
        build:
          commands: ['npm run build --workspace=website']
      artifacts:
        baseDirectory: packages/website/dist/client
        files:
          - '**/*'
      cache:
        paths:
          - 'node_modules'
      buildPath: /
    appRoot: packages/website
